import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Analytics } from "@vercel/analytics/next";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fuel Impact - Fuel calculator",
  description: "Calculate your fuel costs and track expenses with real-time market insights. Get personalized fuel cost analysis and save money on transportation.",
  keywords: "fuel calculator, fuel cost, fuel expense, fuel price, transportation cost, fuel savings",
  authors: [{ name: "Fuel Impact" }],
  openGraph: {
    title: "Fuel Impact - Fuel calculator",
    description: "Calculate your fuel costs and track expenses with real-time market insights.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Analytics />
      </body>
    </html>
  );
}
